[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "deepak28", email = "deepakdobbala22@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "google-adk>=2.8.0",
    "httpx>=0.28.1",
    "mcp<2",
]

[project.scripts]
mcp-server = "mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
